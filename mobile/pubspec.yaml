name: mobile
description: "Ordo — Personal Accountability & Goal Tracking (Flutter)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.1
  http: ^1.3.1
  fl_chart: ^0.70.0
  intl: ^0.19.0
  uuid: ^4.2.1
  supabase_flutter: ^2.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
